const { default: inquirer } = require("inquirer"); // take a look at later
const inquirer = require("inquirer");
const { Circle, Triangle, Square } = require("./lib/shapes");
const fs = require("fs");

function init() {
















}